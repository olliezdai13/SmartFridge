services:
  smartfridge-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartfridge-backend
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
